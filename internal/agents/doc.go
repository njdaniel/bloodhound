// Package agents contains the four agent roles: planner, investigator hounds,
// adversarial verifier, and reporter. Implementations land in M1–M3; the
// contracts they satisfy live in internal/orchestrator.
package agents
